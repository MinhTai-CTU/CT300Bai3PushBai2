function greet(name) {
    return "Hello, ".concat(name, "!");
}
var greeting = greet("World");
console.log(greeting);
// thêm xuống dòng
// thêm dòng for
for (var index = 0; index < 11; index++) {
    console.log(index);
}
